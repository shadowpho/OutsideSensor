{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "scp -p ${workspaceFolder}/* pi@raspberrypi1:~/OutsideSensor/ ; ssh pi@raspberrypi1 'make -C OutsideSensor/out && cd OutsideSensor/out && gdb OutsideSensor' ",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}