{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "scp -p ${workspaceFolder}/* pi@raspberrypi2:~/OutsideSensor/ ; ssh pi@raspberrypi2 'make -C OutsideSensor/out && OutsideSensor/out/OutsideSensor' ",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}